# #1034 / #962 최소 잔여 구현 및 검증 (2026-09-11)

## 결과와 범위
\#1034의 손상된 과거 완료 정보에서 새 체크포인트를 게시하고 UI 작업을 완료하는 경로를 구현했다. #962는 기존 삭제 메뉴와 같은 확인창의 강제 옵션을 재사용하여 최소 탈출 경로를 검증했다. 전체 orphan 조회/자동 정리 UI는 이번 범위가 아니며 #962에 P2 잔여로 남긴다.

기준 HEAD: Cloud `60b9bc42fa`, qemu `4493037`. 두 저장소의 안전 브랜치는 `codex/fix-1034-962-ui-recovery`이다. 기존 통합 개선을 보존한다. 생산 코드 변경은 Cloud의 KVM status wrapper, site broker, DR projection 세 클래스이며 qemu는 설계·검증 문서만 변경한다.

## 구현
- Agent는 과거 completed evidence CONFLICT/INCOMPLETE를 오류로 유지한다. 유효한 별도 pending candidate만 publication 전용 오류 envelope에 보존한다. Plan identity, CLI 종료, 타입, 양수 sequence, producer, disk set을 확인한다.
- 레거시 원격 소스의 빈 active_side는 READY/SYNCING/PAUSED에서만 호환한다. Cloud의 실제 Plan SOURCE 조건과 lifecycle 권한 검증을 유지한다. TARGET 및 전환 terminal은 제외한다.
- Broker는 실패를 성공으로 바꾸지 않고 전달한다. pending이 없더라도 retired worker의 오래된 READY를 되살리지 않는다. healthy live worker 및 lifecycle terminal 우선권을 유지한다.
- Cloud의 누락된 CONFLICT boundary 분류를 추가했다. 새 candidate는 기존 target publish 계약을 통과하고 정확히 같은 COMMITTED proof와 manifestSha256이 확인돼야 ACK한다. 손상 snapshot은 마지막 정상 투영을 덮어쓰지 않는다.

## 모듈 / 회귀
WSL rocky ext4에서 변경 모듈만 `mvn -o -B -pl plugins/hypervisors/kvm,plugins/integrations/ftctl-service,plugins/integrations/disaster-recovery -Dcheckstyle.skip=true package` 수행. 전체 Cloud 빌드는 하지 않았다.
- KVM: 730 tests, 실패/오류 0, skipped 3.
- FTCTL service: 84 tests, 실패/오류 0.
- DR: 457 tests, 실패/오류 0.
- 수정 전 HEAD + 새 테스트는 Agent/broker/projection에서 각각 실패해 원인을 재현했다.
- qemu DR lifecycle 71 cases PASS, release tombstone PASS, checkpoint 13 tests PASS. 셸 변경이 없으므로 qemu 빌드/패키지 배포는 하지 않았다.

## 배포
13/22/31/32 관리 서버 4대와 컴퓨트 12대에 선택 클래스만 반영했다. 기존 JAR 백업과 다른 ZIP entry 동일성을 검증했다. 최종 설치 SHA256이 빌드 클래스와 16대 모두 일치한다. 모든 서비스 active, 관리 UI HTTP 200, WEB-INF 보존, 임시 runtime mask 없음.
- 첫 백업: `/root/issue1034-20260911`.
- 호환성 보완 백업: `/root/issue1034-v2-20260911`.
- 첫 Agent 배포 31.2, 두 번째 31.3에서 stop canceled 경합 재현. 교체 전 중단 후 hash 확인, runtime mask/stop, finally unmask/start로 복원했다. #1010에 기록했다. 이 운영 경합의 근본 해결을 완료했다고 주장하지 않는다.

## #1034 실제 UI 시험
Plan `9a20b190-d202-4b66-9358-b509756f9751` (13 → 31 SharedMountPoint qcow2).
1. 정상 체크포인트 1922에서 기존 status.state와 producer state의 `latest_completed_cycle_token`만 REDACTED로 주입했다. VM/디스크 및 checkpoint artifact는 변경하지 않았다. 원본은 `/root/issue1034-history-fixture-v2/<plan>`에 보존했다.
2. UI 업데이트에서 CONFLICT 오류가 표시됐고 마지막 정상 시각 13:33:15가 유지됐다. 기존 전체 재동기화 메뉴로 요청했다.
3. UI Run `10d7d270-0c5b-4be1-8b2a-36cc7340eb23`은 13:38:09 접수, 13:40:21 `SUCCEEDED / 100%`로 수렴했다. 오류 코드는 비었다.
4. 이전 토큰은 REDACTED 상태로 남아 있는 동안 새 요청 producer의 checkpoint 1924 publication pending이 관찰됐다. 대상 31.1의 COMMITTED proof는 동일 Plan/Run/sequence/디스크 2개와 일치했고 manifest digest를 다시 계산해 검증했다: `fc507f9341582c1626f604cb076df65c1ec57ec5ba514cabc916c06d5e935bdd`.
5. 새 checkpoint 1924 FULL_SEED, TARGET_READY, targetDurableAt 13:39:46, 정상 cycle token, IDLE 확인. 스케줄러 PID 707219 및 worker 42e7c5fd-96ad-4dd6-9af4-3ee30a618cdc 유지.
6. 후속 주기 1925 CBT_INCREMENTAL도 자동 완료하여 READY/HEALTHY/IDLE로 복귀했다.
7. 이 최종 요청 이후 서비스 재시작, runtime 상태 보정, DB repair, 수동 ACK를 하지 않았다.

초기 보완 전 시험 Run b5f501e5-2271-450c-bbc7-022bdabd21fc는 빈 active_side 및 오래된 호스트 fallback 때문에 FAILED였고 PASS에 포함하지 않는다. 실패 이력은 보존했다.

RBD Plan `7ec74483-8554-415d-ac56-f62f8b17fbd0`은 UI checkpoint 4682, VMware Plan `a85874ae-d1bd-470b-97c5-7c48a39486dd`은 checkpoint 250 이후 runtime 251까지 정상 증분 복제 확인. 둘 다 READY/HEALTHY/IDLE이며 기존 worker PID 유지. VMware 원본·대상 QGA는 사용하지 않았다.

실제 손상 주입부터 UI 요청 완료까지의 시험은 qcow2 경로에서 수행했다. RBD/VMware는 배포 후 정상 증분/상태 회귀이며, 이번에 물리 호스트 장애·전체 failover/failback·마이그레이션 전 조합을 새로 반복한 것은 아니다. 이 잔여 통합 행렬은 #922/#950에서 관리한다.

## #962 최소 UI 시험
31 클러스터에 작업자가 없는 폐기용 메타데이터 fixture를 생성했다. 소스는 닫힌 localhost 포트/인증 정보 없음, ERROR/DISABLED, 실패 SYNC 이력 상태다. 실제 VM/볼륨은 새로 만들거나 연결하지 않았다.
- 자원 흔적 없는 첫 fixture는 일반 삭제 성공 (job 4079).
- target-ready 흔적이 있는 두 번째 fixture `2fb14e94-52c9-4551-9647-8111911634c7`은 삭제 메뉴 활성, 일반 삭제 job 4080은 DR_RUNTIME_RESOURCE_EXISTS로 거절.
- 같은 확인창에서 강제를 선택한 job 4081은 성공. Plan soft-delete 및 DISABLED, runtime 행과 FAILED Run 이력 보존 확인. 초기 fixture status_json은 UI 투영 과정에서 not_found로 갱신됐으므로 원본 marker 문자열 보존을 주장하지 않는다.
- VM 27대/볼륨 38개 수 불변. 폐기용 사이트 2개도 UI에서 제거했다. 실제 원격 orphan 파일 자동 정리 기능을 시험한 것은 아니다.
- 기존 활성 Run 거절 및 명시적 취소/종료 요구 단위 테스트도 통과했다. 작업 중 원격 쓰기를 강제로 성공/정리로 위장하지 않는다.

## 남은 이슈 / 우선순위
Nutanix #956~#959 제외. #1034는 구현/핵심 시험 완료 트랙으로 이동하고 #962 확장 범위는 P2로 유지한다.
1. P1 #967: 요청 FULL_RESEED와 Run 완료의 일치 여부 재현/잔여 경합 확인.
2. P1 #980: 테스트 cleanup 후 RESUME 응답 유실 재시도의 멱등성.
3. P1 #954: 사용자 정의 offering의 CPU/Memory 입력 및 Plan 생성.
4. 별도 P1 #1029: Diplo DB 연결 누수 이식.
5. P2 #962 확장, #1021/#1018 증거 정확성, #1006 보존/수동 정리, #951→#952→#953 자원 경계, #1001/#986 복구 안내, #973/#966/#1019/#1020 표시, #1028/#1027 DB/로깅, #1033/#978/#1010/qemu #55 배포·회귀 지원.
6. P3 #955 실행별 사양 override.

개발/분석/잔여 보완 24건(Cloud 23 + qemu 1), 구현 및 핵심 시험 완료/병합·잔여 검증 31건. PR #1022의 기존 upstream 충돌 해소와 paired 최종 병합 검증은 별도 트랙이며 이번 작업에서 병합하지 않는다.
